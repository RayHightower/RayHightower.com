source "https://rubygems.org"

gem "jekyll", "~> 4.3.3" # Specifies the Jekyll version
gem "minima", "~> 2.5" # The theme gem (optional, can be removed if not used)
gem "jekyll-feed" # Plugin for RSS feed generation
gem "jekyll-sitemap" # Plugin for sitemap generation
gem "jekyll-seo-tag" # Plugin for SEO metadata

group :jekyll_plugins do
  # Additional Jekyll plugins can be added here if needed
end